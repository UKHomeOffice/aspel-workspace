{
  "name": "@ukhomeoffice/aspel_workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "version": "1.0.1",
  "scripts": {
    "reset": "./scripts/reset-aspel-workspace.sh",
    "test": "node --test $(find . -type f -name \"*.test.js\" ! -path \"./node_modules/*\" ! -path \"./packages/*\")",
    "echo": "echo 'workspace is in action'"
  }
}
