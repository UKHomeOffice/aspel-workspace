module.exports = {
  // The process for adding roles to a person within an establishment, e.g. NACWO, NVS
  role: {
    // Moved to collecting structured data from multiple inputs rather than a free text field
    NAMED_PERSON_VERSION_ID: 2
  }
}
