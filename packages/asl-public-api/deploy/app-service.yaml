apiVersion: v1
kind: Service
metadata:
  name: asl-dev-pubapi
  labels:
    name: asl-dev-pubapi
    role: service
spec:
  ports:
  - name: http
    port: 10080
  - name: https
    port: 10443
  selector:
    name: asl-dev-pubapi
