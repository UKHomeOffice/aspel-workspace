apiVersion: extensions/v1beta1
kind: NetworkPolicy
metadata:
  name: pubapi-permit-ingress
spec:
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: ingress-external
    ports:
    - port: 10080
      protocol: TCP
    - port: 10443
      protocol: TCP
  podSelector:
    matchLabels:
      name: asl-dev-pubapi
  policyTypes:
  - Ingress