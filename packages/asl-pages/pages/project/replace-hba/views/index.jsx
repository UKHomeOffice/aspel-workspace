import { connect } from 'react-redux';
import React from 'react';

const ReplaceHba = () => {
  return (
    <React.Fragment>
      <h1>ReplaceHBA</h1>
    </React.Fragment>
  );
};

const mapStateToProps = () => ({});

export default connect(mapStateToProps)(ReplaceHba);
